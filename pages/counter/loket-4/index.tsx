import LoketInterface from '@/components/loket/LoketInterface';
import { LoketLayout } from '@/components/layout/LoketLayout';

export default function Loket4Page() {
  return (
    <LoketLayout loketId={4}>
      <LoketInterface loketId={4} />
    </LoketLayout>
  );
}
